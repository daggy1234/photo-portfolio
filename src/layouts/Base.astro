---
import '../styles/global.css';
import Nav from '../components/Nav.astro';

const {
  title,
  description,
  active = 'home',
  captionLeft = '',
  captionRight = '',
  preload = null, // { srcset, sizes } for the likely LCP image
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
    <meta name="generator" content={Astro.generator} />
    <link
      rel="preload"
      href="/fonts/source-serif-4-latin-700-normal.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
    {
      preload && (
        <link
          rel="preload"
          as="image"
          imagesrcset={preload.srcset}
          imagesizes={preload.sizes}
          fetchpriority="high"
        />
      )
    }
    <script is:inline>
      // Pre-paint theme: localStorage first, then OS preference. Keep inline
      // and first so there is no flash of the wrong theme.
      (function () {
        try {
          var t =
            localStorage.getItem('pf-theme') ||
            (matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
          document.documentElement.dataset.theme = t;
        } catch (e) {}
      })();
    </script>
  </head>
  <body>
    <Nav {active} {captionLeft} {captionRight} />
    <slot />
  </body>
</html>
